package application;

import javafx.scene.control.TableRow;
import javafx.scene.control.TableView;
import javafx.util.Callback;

public class Highlighter {

	public void highlight(){
		/*MyController.tableView.setRowFactory(new Callback<TableView<ValueClass>, TableRow<ValueClass>>() {
	    @Override
	    public TableRow<ValueClass> call(TableView<ValueClass> param) {
	    	final TableRow<ValueClass> row = new TableRow<ValueClass>() {

	            @Override
	            protected void updateItem(ValueClass line, boolean empty) {
	                super.updateItem(line, empty);

	              if (!empty) {
	            	  	if(line.getColumn1().equals("0001"))
	            	  		setStyle("-fx-background-color: yellow");               
	            	  	else {
	            	  		setStyle("");
	            	  	}

	              } else {
	            	  setStyle("");
	              }
	            }
	        };
			return row;
	    }
	});*/
	}
}
